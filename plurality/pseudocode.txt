for all votes
    if name == candidate.name
        {
            update candidate vote count
            return true;
        }

    else if(name != candidate.name)
        {
            printf("Invalid ballot\n");
            return false;
        }

candidate with most votes

if multiple candidates == max votes
    {
        printf("candidates.names)
    }