#include <cs50.h>
#include <stdio.h>

int main(void)
{
    for (int i = 0; i <3; i+=1) // three arguments. first int i = 0 initiates. Second is a boolean condition i <3 which is being checked. Finally, i+=1 tells loop to increment by one each time the loop runs.
    printf("meow\n");
}