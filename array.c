#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int length
    do
    {
        length = get_int("Length: ");
    }
    while (length < 1);
}

int dynamic_array[length]
{
    for (i = 0; i < length, i++)
    {
       int dynamic_array[0] = 1;
       int dynamic_array[1] = 
    }
}