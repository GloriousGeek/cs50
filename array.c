#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int length
    do
    {
        length = get_int("Length: ");
    }
    while (length < 1);
}

int dynamic_array[length]
{
    // Initial value of array should be 1
    int start_value[0] = 1;

    for (i = 0; i < length, i++)
    {
        printf("%i\n", start_value);


    }
}