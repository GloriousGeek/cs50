#include <cs50.h>
#include <stdio.c>
#include <string.h>

int main(void)
{
    string name = get_string("Name: ");

    int length = strlen(name);

    for (int i = 0; i < length - 1; i++)
    {
        
    }
}